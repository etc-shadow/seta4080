<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.seta4080">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SETA4080">
        <activity android:name=".ImageMCQuiz"></activity>
        <activity android:name=".ImageTextActivity" />
        <activity
            android:name=".VideoPlayback"
            android:label="@string/title_activity_video_playback"
            android:theme="@style/Theme.SETA4080.NoActionBar" />
        <activity android:name=".FinishScreen" />
        <activity
            android:name=".TextInfo"
            android:parentActivityName=".SelectModule" />
        <activity
            android:name=".MCQuiz"
            android:parentActivityName=".SelectModule" />
        <activity
            android:name=".SelectModule"
            android:parentActivityName=".MainActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>